<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.akbar.taufik.urbanfarming.ListTanaman"
    tools:showIn="@layout/activity_list_tanaman">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/listTanaman"
        android:dividerHeight="0dp"
        android:divider="@null"
        android:scrollbars="none"
        android:padding="8dp"
        android:clipToPadding="false">
    </ListView>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="It seems like you don't have any plants yet! Try to add one by tapping the + button on the bottom right corner."
        android:textAlignment="center"
        android:padding="16dp"
        android:textSize="24dp"
        android:textColor="@android:color/darker_gray"
        android:gravity="center_vertical"
        android:id="@+id/noPlantText"
        android:visibility="invisible"/>

</RelativeLayout>
